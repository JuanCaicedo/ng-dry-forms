angular.module('angular-dry-forms').controller('editItemController', [
    '$scope',
    '$http',
    function EditItemController($scope, $http) {
        //Controller logic outside the directive
    }
]);